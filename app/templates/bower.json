{
  "name": "<%= name %>",
  "private": true,
  "dependencies": {<% -%>
    <% vendors.forEach(function(vendor, i) { %>
    "<%= vendor %>": "0.0.0"<% if (i !== (vendors.length - 1)) { %>,<% } -%>
    <% }); %>
  }
}